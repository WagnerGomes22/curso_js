let semTexto = document.querySelector(".dom");
let novoTexto = document.createTextNode("esse é o novo texto");

semTexto.appendChild(novoTexto);

// na solução acima adicionamos um texto no HTMl, basicamente fizemos um no, se tiver texto no html eles irão fazer um no de texto(juntar os textos)